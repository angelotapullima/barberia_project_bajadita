-- Comandos SQL para eliminar DEFAULT NOW() de las columnas de fecha y hora
-- Esto permitirá que el backend controle explícitamente los valores de estas columnas.

-- Eliminar DEFAULT NOW() para created_at

-- Eliminar DEFAULT NOW() para updated_at

-- Eliminar DEFAULT NOW() para sale_date

-- Eliminar DEFAULT NOW() para movement_date (en inventory_movements)
